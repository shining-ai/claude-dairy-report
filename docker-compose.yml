services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: dairy
      POSTGRES_PASSWORD: dairy
      POSTGRES_DB: dairy_report_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
